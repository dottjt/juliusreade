box: node

build:
  steps:
    - npm-install
    - hgen/gulp:
        tasks: minic
    - arjen/hugo-build:
        version: "0.16"
        theme: reade
    - hgen/gulp:
        tasks: inc


deploy:
  steps:
    - s3sync:
        source-dir: public/
        delete-removed: true
        bucket-url: $BUCKET
        key-id: $KEY
        key-secret: $SECRET
        opts: --acl-public --add-header=Cache-Control:max-age=3600

